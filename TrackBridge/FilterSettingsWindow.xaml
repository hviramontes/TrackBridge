<Window x:Class="TrackBridge.FilterSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Filter Settings"
        Height="350" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Domains label -->
            <RowDefinition Height="*"/>
            <!-- Domains checkboxes -->
            <RowDefinition Height="Auto"/>
            <!-- Kinds label -->
            <RowDefinition Height="*"/>
            <!-- Kinds checkboxes -->
            <RowDefinition Height="Auto"/>
            <!-- PublishOnly -->
            <RowDefinition Height="Auto"/>
            <!-- Stale Threshold -->
            <RowDefinition Height="Auto"/>
            <!-- Buttons -->
        </Grid.RowDefinitions>

        <!-- Domains -->
        <TextBlock Grid.Row="0" FontWeight="Bold">Allowed Domains</TextBlock>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="DomainPanel" />
        </ScrollViewer>

        <!-- Kinds -->
        <TextBlock Grid.Row="2" FontWeight="Bold" Margin="0,10,0,0">Allowed Kinds</TextBlock>
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="KindPanel" />
        </ScrollViewer>

        <!-- Publish Only -->
        <CheckBox x:Name="PublishOnlyCheckBox"
                  Grid.Row="4"
                  Margin="0,10,0,0"
                  Content="Publish Only"/>

        <!-- Stale Threshold -->
        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    Margin="0,10,0,0">
            <TextBlock Text="Stale Threshold (sec):"
                       VerticalAlignment="Center"
                       Margin="0,0,5,0"/>
            <TextBox x:Name="StaleThresholdTextBox"
                     Width="50"
                     VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="6"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button x:Name="SaveButton"
                    Width="75"
                    Margin="0,0,5,0"
                    Content="Save"
                    Click="Save_Click"/>
            <Button x:Name="CancelButton"
                    Width="75"
                    Content="Cancel"
                    Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
