<Window x:Class="TrackBridge.TrackDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Track Details" Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Entity ID:" FontWeight="Bold"/>
        <TextBox x:Name="TxtEntityId" Grid.Row="0" Margin="100,0,0,5" IsReadOnly="True"/>

        <TextBlock Text="Current Marking:" FontWeight="Bold" Grid.Row="1"/>
        <TextBox x:Name="TxtMarking" Grid.Row="1" Margin="100,0,0,5"/>

        <CheckBox x:Name="ChkLock" Grid.Row="2" Margin="100,5,0,5" Content="Lock this marking" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Save" Width="75" Margin="0,0,10,0" Click="Save_Click"/>
            <Button Content="Cancel" Width="75" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
